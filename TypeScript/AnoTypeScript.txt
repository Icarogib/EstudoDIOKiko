//por motivos de ser muito grande, eu apaguei a pasta modules do TS.
//esta ai so a base de cada programa


npm init -cria projeto node.

npm install typescript - instala TS

npm install lite-server - teste server live

npm start - inicia o projeto, para rodar com lite server, e watch, se coloca no 
scripts json: 
$  "start": "lite-server",
"watch": "tsc --watch"

tsc --init - cria um arquivo configuracao TS.

para rodar -> npm run watch